apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-deployment
  namespace: lastmile
  labels:
    app: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: imsonu2030/lastmile-user-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: SUPABASE_URL
          valueFrom:
            secretKeyRef:
              name: lastmile-secrets
              key: supabase_url
        - name: SUPABASE_KEY
          valueFrom:
            secretKeyRef:
              name: lastmile-secrets
              key: supabase_key